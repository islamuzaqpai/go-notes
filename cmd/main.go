package main

func main() {
	//conn, err := db.Connect()
	//if err != nil {
	//	log.Fatalf("Error connect to db: %v", err)
	//}
	//defer conn.Close(context.Background())
	//
	////err = db.InsertNote(conn, "First Note", "This is my first note written in Go!")
	//if err != nil {
	//	log.Fatalf("Error Insert Note: %w", err)
	//}
	//
	//notes, err := db.GetNotes(conn)
	//if err != nil {
	//	log.Fatalf("Error to getting notes: %v", err)
	//}
	//
	//fmt.Println("All Notes:")
	//for _, note := range notes {
	//	fmt.Printf("- %d | %s | %s\n", note.ID, note.Title, note.Content)
	//}
}
